test: cover calculateGrayDiff frame diff algorithm (S2 gap)

Add 8 tests for the core screen monitoring frame difference algorithm:

- Same frame returns 0%
- Full black vs full white returns 100%
- Mismatched lengths return 100%
- Single pixel difference precise calculation
- Half black half white returns ~50%
- Symmetry verification (a vs b === b vs a)
- Empty arrays return 0% (NaN bug fix)
- Random input result in 0-100 range

Bug fix: calculateGrayDiff([], []) previously returned NaN due to 0/0.
Added empty array guard to return 0 directly.

Source changes:
- Export calculateGrayDiff and extractGrayFrame from screen-monitor.ts
- Add empty array boundary protection in calculateGrayDiff

All 17 tests pass (9 clamping + 8 frame diff). Typecheck clean.